import 'package:flutter/material.dart';

class ContenidoCrudScreen extends StatelessWidget {
  const ContenidoCrudScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Contenido CRUD')),
      body: const Center(child: Text('Pantalla en construcción')),
    );
  }
}