import 'package:flutter/material.dart';

class GestanteFormMejoradoScreen extends StatelessWidget {
  const GestanteFormMejoradoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Nueva Gestante')),
      body: const Center(child: Text('Formulario de gestante (en construcción)')),
    );
  }
}